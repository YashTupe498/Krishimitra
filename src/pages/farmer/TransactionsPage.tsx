import React, { useEffect, useState } from 'react';
import { Card } from '../../components/ui/Card'; import { marketplaceService } from '../../services/mockMarketplace'; import type { MarketplaceTransaction } from '../../types/marketplace';
export const FarmerTransactionsPage: React.FC = () => { const [transactions, setTransactions] = useState<MarketplaceTransaction[]>([]); useEffect(() => { marketplaceService.getTransactionsByFarmer().then(setTransactions); }, []); return <div className="space-y-4"><h1 className="h2">Sale Transactions</h1>{transactions.length === 0 && <Card className="p-6">No active transactions.</Card>}{transactions.map((txn) => <Card key={txn.id} className="p-6"><strong>{txn.id}</strong><p>{txn.quantity.toLocaleString()} kg · ₹{txn.totalValue.toLocaleString()} · {txn.transactionStatus}</p></Card>)}</div>; };
